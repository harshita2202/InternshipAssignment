## 1. If this were evolved into a production QDC system then we requrie a proper database to handle large data (like MongoDB or PostgreSQL) depends the kind of data you are storing , this will ensure that the data didn't disapper when the server restarts . We can use feature like indexing , pagunation and transactions to improve the performance and it will ensure data consistenct when many user place order at same time . We can also use cashing so that the database load can be reduce this methord can be used for frequently accessed data.

## 2. Current approach is simple but not professional it creates inconsistent API responses and makes frontend error handling more difficult as cliten dosen't know the real error. In a real-world REST API, I would use proper HTTP status codes and NestJS exceptions instead of returning a plain error object. This improves API consistency and frontend error handling.

## 3. To imporve maintainability , I would write separate API logic into dedicated service files or custom hooks. I would organize and divide frontend into components, pages, hooks and api. I would also use tools such as Axios for API management, caching, and loading states. For larger applications, state management solutions like Context API could help manage shared state more effectively.

## 4. some important missing fields for real-world are payment status, delivery address , pickup date , garment price and some edge cases like lost garments , cancelled order , delayed delivery , missdeliveried order . I would expand the domain model with additional entities such as invoices, payments, delivery tracking, customers, and audit logs for status history. This would make the system more flexible and scalable for actual business operations.

## 5. If AI wrote this code there can be hidden bugs , broken code , security issues , bad architecture . SO , before shipping AI-generated code to production, I would do code reviews , test ad check APIs call manually , validate inputes and fix bugs if found any.

## 6. currently REST APIs give code when frontend requests it , so real time update I could use WebSockets or Socket.IO , the backend could push updates instantly whenever a garment status changes. However, WebSockets also increase system complexity because the server must manage persistent client connections. Scaling real-time systems can also become more challenging. An alternative approach would be polling the REST API every few seconds, which is simpler to implement but less efficient than real-time push updates.